# ff14-macro-helper Specification

## 目的と優先順位

1. 日本語版 FFXIV マクロを、制約を守りながら素早く編集・コピーできる。
2. 公式テキストコマンドを、検索・補完・説明で見つけやすくする。
3. ゲーム内ログの概形をプレビューし、実行前の読み違いを減らす。
4. URL だけでマクロ本文を共有・復元できる。

## データモデル

```ts
type MacroDocument = {
  version: 1
  body: string
}

type CommandCategory =
  | 'chat'
  | 'party_social'
  | 'target'
  | 'action_hotbar'
  | 'battle'
  | 'system'
  | 'macro'
  | 'config'
  | 'emote'
  | 'menu'
  | 'pronoun'

type SupportLevel = 'reference_only' | 'lint' | 'log_preview'

type CommandDefinition = {
  id: string
  names: string[]
  category: CommandCategory
  signature: string
  description: string
  sourceUrl: string
  verifiedAt: string
  gameVersion: string
  support: SupportLevel
  rules: CommandRule[]
}

type Diagnostic = {
  severity: 'error' | 'warning' | 'info'
  line: number
  column?: number
  code: string
  message: string
}

type LogEntry = {
  line: number
  kind: 'shout' | 'party' | 'echo' | 'action' | 'system' | 'error' | 'unknown'
  text: string
  isPreview: true
}
```

`CommandRule` は引数数、数値範囲、重複不可、文脈依存など、辞書から評価できるルールを表す。ゲーム状態が必要な判定はこの型で無理に表現せず、注意情報として扱う。

## 入力と URL

### エディタ入力

- 入力は改行で分割し、各行の元の文字列を保持する。
- コピー用の本文はユーザーが入力した改行・空白を保持する。解析用だけ別途正規化してよい。
- コマンド名は先頭の `/` から最初の空白までとし、辞書の正式名・短縮名と大文字小文字を区別せず照合する。
- `/a` のような途中入力には、一致する正式名と短縮名の補完候補を返す。

### 共有 URL

- 本文はバージョン付きの `MacroDocument` として可逆にエンコードし、URL クエリに埋め込む。
- URL を読み込めない、形式が不正、将来の未対応バージョンである場合は、内容を実行・コピーせず理由を表示する。
- URL がブラウザや共有先の実用上の長さを超えうる場合は、短縮保存機能を追加する前に、明確な長さ警告と代替コピー手段を表示する。
- URL は共有の正本であり、MVP でアカウントやサーバー保存を要求しない。

## バリデーション

| ルール | 判定 | 重要度 |
| --- | --- | --- |
| 行数 | 改行で区切った行が 15 行を超える | error |
| 行長 | 1 行が 180 文字を超える | error |
| コマンド | `/` で始まる行のコマンドが辞書にない | warning |
| 引数 | 対応済みコマンドが必須引数・数値範囲・重複制約に反する | error または warning |
| 非対応 | 辞書にはあるが lint／ログ未対応 | info |
| URL | 共有データの復元に失敗する | error |

文字数は JavaScript の UTF-16 コードユニット数ではなく、ゲーム内制限と一致する単位を実機検証してから固定する。検証前は「推定」と表示し、実機との比較ケースをテストに残す。

## 補完とチップヘルプ

- カーソル位置が行頭のコマンド範囲にある時だけコマンド補完を出す。
- `/a` は `/ac` と `/action` のように短縮名・正式名の前方一致を候補にする。
- 候補は入力との一致度、短縮名、マクロでの有用性を基準に並べる。カテゴリと説明を添える。
- 選択中またはカーソル位置のコマンドには、構文、説明、別名、公式リンク、対応範囲を表示する。
- 引数の補完はコマンド辞書に確実な候補がある場合だけ提供する。アクション名等の不完全なデータを推測補完しない。

## ログプレビュー

- 行を上から順に変換し、対応済みコマンドは種類ごとの視覚表現で出力する。
- `/sh` はエリア向け発言、`/p` はパーティー発言、`/echo` は自分向け発言、`/ac` は「スキル：{名前}を発動」の形式を初期対応とする。
- `/wait` は待機時間を示すシステム行を表示する。時間の経過を正確に再現する必要はない。
- 構文エラーは赤系のエラー行として表示し、元の行番号と診断内容を紐付ける。
- 未対応またはゲーム状態依存のコマンドは、入力行を隠さず「このプレビューでは再現できない」と表示する。
- 出力はゲーム内の実行結果ではなく、読み合わせ用のテキストログである。

## コマンド辞書の整備

- 公式テキストコマンド一覧を母集団とし、カテゴリ・正式名・短縮名・構文・説明・公式 URL を収録対象にする。
- MVP では全件を **参照・検索・補完の候補** として扱うことを目標にする。
- lint とログプレビューは、マクロ専用、チャット、アクション／ホットバー、その他の利用頻度が低い実用コマンドから優先順位を付けて広げる。
- 取得・整理を AI が補助しても、公開前に公式原文との照合を行う。曖昧または再現不能な仕様はサポートレベルを下げ、個別判断を記録する。

## 表示仕様

- PC ブラウザでは、編集、診断、ヘルプ、ログ、コピー／共有を互いに行き来せず確認できる配置にする。
- コピー後は成功・失敗を明示し、コピー対象が診断エラーを含む時も阻害せず、警告を理解できる状態で実行させる。
- エラーには色に加えてアイコン、本文、対象行を表示する。
- モバイルでの編集最適化は MVP の対象外だが、閲覧不能な固定幅にはしない。

## 非機能要件

- 最新版の Chrome、Safari、Edge の PC ブラウザで利用できる。
- 入力、診断、補完はネットワーク接続なしで応答する。
- コマンド辞書の出典と確認日を表示できる。
- 公開前に、制限値・構文・URL 復元・補完・ログ変換の単体テストを用意する。

