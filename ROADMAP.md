# ff14-macro-helper Roadmap

## v1: PC 向けマクロエディタ

- 15 行・180 文字／行の制約を可視化するテキストエディタを作る。
- コピーと、本文を URL クエリへ埋め込む共有・復元を実装する。
- 公式テキストコマンドを基に、検索・補完・チップヘルプ用の日本語コマンド辞書を整備する。
- 正式名・短縮名の補完、構文ハイライト、未知コマンド・基本引数の lint を実装する。
- `/sh`、`/p`、`/echo`、`/ac`、`/wait` とエラー表示のログプレビューを実装する。
- 辞書にある全コマンドを参照できるようにし、lint／ログプレビュー未対応は明示する。

## 申し送り（2026-09-09 セッション終了時点）

ログプレビューのチャンネル別配色・タイムスタンプ・プレースホルダ解決（`<t>` 等）は実装済み
（`src/lib/macro/log-preview.ts` `log-placeholder.ts`）。詳しい代入ルールと実機確認の経緯は
`docs/draft/placeholder-mapping.md` を参照。次に着手すべきものは以下の2件、どちらも
補完・ハイライト側の作業が一段落してから、新しいセッションで着手するのがおすすめ
（同じ `MacroWorkbench.tsx` を並行編集すると競合整理の手間が発生するため）。

1. **レイアウト再編＋「マクロ再生」ボタン**（次の主タスク）
   - 左にエディタ、右にサジェスト＋診断、下に実行ログを時系列で流すプレビュー、という配置に変更したい（渚さん希望）。
   - 再生ボタン押下で、ログプレビューを1行ずつ `/wait` の秒数に応じて時間差で表示するアニメーション的な挙動を追加する。
   - `toLogPreview` が返す `timestamp`（現在時刻起点の疑似経過時間）と `segments`（プレースホルダのバッジ表示）はそのまま再利用できる設計にしてある。

2. **`/action` のプレースホルダ解決（実機確認待ち）**
   - 現状 `log-preview.ts` の `/action` `/ac` 分岐は `スキル：Xを発動` という素朴な文字列組み立てのみで、`<t>` 等のプレースホルダ解決を一切行っていない（意図的にスコープ外にした）。
   - 渚さんの記憶ベースだと、実際は `/ac かばう <2>` のようなターゲット指定があっても、ログには対象は出ず「**YourName**のかばう」のような形だけが表示される、とのこと（未確認、後日実機で確認予定）。
   - 確認が取れ次第、`log-placeholder.ts` の `resolveLogText` を使って書き直す。

   その他、未検証のまま残っている項目（`docs/draft/placeholder-mapping.md` にも記載）:
   - `pvpteam`（`[PT]`表示だったという情報が一度出たが撤回。現在は prefix なしで暫定運用）
   - `beginner`（`[BG]`表示は未検証の推測だったため prefix なしに戻し済み）

## 次段階

- 代表的でないコマンドを含む lint とログプレビューの対応範囲を段階的に広げる。
- コマンド辞書の更新差分をレビューする運用と、ゲームバージョン別の状態表示を整備する。
- URL が長い場合の短縮共有を Neon ベースで追加する。
- 保存済みマクロ、共有マクロの公開、検索を追加する。
- モバイル向けの閲覧・編集体験を最適化する。
- 正引き・逆引きのマクロガイドと、ドラッグ&ドロップでの GUI 組み立てを追加する。

## 今はやらないこと

- FFXIV クライアントとの連携、自動入力、自動実行
- 戦闘状態・対象・ジョブ・設定を再現する完全シミュレータ
- アカウント、共同編集、コメント、ソーシャル機能
- 多言語のコマンド辞書とローカライズ
- すべてのコマンドに同じ深さの引数検証とログ再現を一度に実装すること

## 実装の順序

1. 実機のマクロ制限と文字数の数え方を、代表ケースで確認してテスト化する。
2. コマンド辞書のスキーマと、公式情報を取り込んでレビューする手順を固める。
3. 解析・診断・エディタ・コピー・URL 復元を、ネットワークなしで動かす。
4. 補完・チップヘルプ・構文ハイライトを同じ解析結果に接続する。
5. 初期対象コマンドのログプレビューを実装し、未対応時の表示を整える。
6. 実際のマクロ例を使い、ゲーム内貼り付けと URL 共有を検証する。

